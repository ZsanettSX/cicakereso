**Checkbox** — rounded checkbox with forest-green fill. For filter toggles like "Csak oltott cicák".

```jsx
<Checkbox label="Csak ivartalanított" checked={v} onChange={e => set(e.target.checked)} />
```
