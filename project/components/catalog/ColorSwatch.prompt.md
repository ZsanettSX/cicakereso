**ColorSwatch** — coat-colour chip for the "szín" filter and cat profiles. Pass a `--cat-*` token.

```jsx
<ColorSwatch color="var(--cat-orange)" label="Vörös" selected onClick={...} />
```

Sizes `sm`/`md`/`lg`. Selected shows a forest ring. Omit `onClick` for a static display swatch.
